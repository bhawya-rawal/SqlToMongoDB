# PostgreSQL connection (required for optimizer/EXPLAIN features)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=TPC-H
DB_USER=postgres
DB_PASSWORD=postgres

# API port
PORT=5000

# Optional timeouts (ms)
DB_CONNECTION_TIMEOUT_MS=5000
DB_QUERY_TIMEOUT_MS=15000
DB_IDLE_TIMEOUT_MS=30000
